/* firebase-config.js — paste the firebaseConfig object from
   Firebase Console → Project settings → General → Your apps → Web app.
   Safe to commit: Firebase web API keys are not secrets; access control
   lives in Firestore security rules (see firestore.rules), not here. */
window.CCS_FIREBASE_CONFIG = {
  apiKey: "PASTE_ME",
  authDomain: "PASTE_ME",
  projectId: "PASTE_ME",
  storageBucket: "PASTE_ME",
  messagingSenderId: "PASTE_ME",
  appId: "PASTE_ME"
};
